<!-- GENERATED — identity-agnostic release view of the methodology handbook. Single source; regenerate via `yitc-v2 graph release-view`, do not edit. -->

<!-- GENERATED by `bin/yitc-v2 graph build` from the per-spec `binding:` field.
     DO NOT EDIT — hand edits are overwritten on the next build. Single canonical carrier:
     the `binding:` field on each spec; this map is a derived-only view (plan-check finding 1). -->

# Floor trigger-map — read BEFORE the gated action (always-loaded tier)

The mandatory-on-entry triggers (the obligation set, Part I-A). Before the named action,
read the listed `retrieved` spec(s) via `bin/yitc-v2 graph query --kernel <SPEC-ID>` — the
kernel-explicit form (SPEC-0092), because a bare id resolves consumer-own-first in a `-C`
session and this project may own a spec at the same id.

- **before-rule-change** (`spec-new`) → read `bin/yitc-v2 graph query --kernel SPEC-0005` — The spec doctrine — identity, content boundary, FSM, homing, and how to create/split/reshape a spec
- **before-rule-change** (`spec-new`) → read `bin/yitc-v2 graph query --kernel SPEC-0073` — Corpus-wide placement contract — every yitc artifact+code classified kernel|v2-self|project at authoring
- **before-rule-change** (`spec-new`) → read `bin/yitc-v2 graph query --kernel SPEC-0128` — Seed-and-grow concern registry — one authoritative concern declaration; all surfaces generated from it
- **before-rule-change** (`spec-edit`) → read `bin/yitc-v2 graph query --kernel SPEC-0005` — The spec doctrine — identity, content boundary, FSM, homing, and how to create/split/reshape a spec
- **before-authoring-interaction** (`before-authoring-interaction`) → read `bin/yitc-v2 graph query --kernel SPEC-0100` — Behavioral-baseline default catalog — at-the-moment delivery of the seeded cross-project behavioral/UX-safety defaults (before-authoring-interaction)
- **before-project-dimension-judgement** (`before-project-dimension-judgement`) → read `bin/yitc-v2 graph query --kernel SPEC-0073` — Corpus-wide placement contract — every yitc artifact+code classified kernel|v2-self|project at authoring
- **before-project-dimension-judgement** (`before-project-dimension-judgement`) → read `bin/yitc-v2 graph query --kernel SPEC-0090` — The lesson node type — a per-repo non-traveling craft-knowledge entity + the patterns(travel)/lessons(local) boundary
- **before-project-dimension-judgement** (`before-project-dimension-judgement`) → read `bin/yitc-v2 graph query --kernel SPEC-0093` — yitc-ops.yaml project ops-contract carrier — sections, declare-or-waive, slice-1 boundary
- **before-project-dimension-judgement** (`before-project-dimension-judgement`) → read `bin/yitc-v2 graph query --kernel SPEC-0122` — External shared-library dependency — govern a consumer's fix by pinned-dependency-contract, escalate to register-as-project
- **before-project-dimension-judgement** (`before-project-dimension-judgement`) → read `bin/yitc-v2 graph query --kernel SPEC-0125` — Consumer vendor-adapter doctrine — thin <vendor-adapter>.md + provider-neutral project-context home
- **before-deploy** (`before-deploy`) → read `bin/yitc-v2 graph query --kernel SPEC-0097` — Deploy-autonomy policy — reversibility/RTO-RPO A/S/C1/C2 deploy classification + per-class evidence gate — done via `bin/yitc-v2 -C <path> deploy --class {A|S|C1|C2}`
- **before-external-mutation** (`before-external-mutation`) → read `bin/yitc-v2 graph query --kernel SPEC-0116` — External-mutation capture — external_action event for a substantive outbound mutation lacking a dedicated verb
- **before-mcp-use** (`before-mcp-use`) → read `bin/yitc-v2 graph query --kernel SPEC-0118` — Working with MCP in v2 — two-shelf model + session-start reconcile + capture-delivery contract (before-mcp-use)
- **before-scheduling-a-cadence** (`before-scheduling-a-cadence`) → read `bin/yitc-v2 graph query --kernel SPEC-0142` — Operational provider-independence — a governed recurring obligation names a provider-independent trigger; AI judgement is separable and non-gating
- **before-handling-a-secret** (`before-handling-a-secret`) → read `bin/yitc-v2 graph query --kernel SPEC-0100` — Behavioral-baseline default catalog — at-the-moment delivery of the seeded cross-project behavioral/UX-safety defaults (before-authoring-interaction)
- **before-authoring-auth** (`before-authoring-auth`) → read `bin/yitc-v2 graph query --kernel SPEC-0100` — Behavioral-baseline default catalog — at-the-moment delivery of the seeded cross-project behavioral/UX-safety defaults (before-authoring-interaction)
- **before-restoring-a-production-copy** (`before-restoring-a-production-copy`) → read `bin/yitc-v2 graph query --kernel SPEC-0175` — Spike-sandbox data floor — a restored production copy is the sanctioned source, treated on three strands before the stack is usable
- **before-authoring-public-boundary** (`before-authoring-public-boundary`) → read `bin/yitc-v2 graph query --kernel SPEC-0100` — Behavioral-baseline default catalog — at-the-moment delivery of the seeded cross-project behavioral/UX-safety defaults (before-authoring-interaction)
- **before-adding-a-dependency** (`before-adding-a-dependency`) → read `bin/yitc-v2 graph query --kernel SPEC-0100` — Behavioral-baseline default catalog — at-the-moment delivery of the seeded cross-project behavioral/UX-safety defaults (before-authoring-interaction)
- **before-authoring-UI** (`before-authoring-UI`) → read `bin/yitc-v2 graph query --kernel SPEC-0100` — Behavioral-baseline default catalog — at-the-moment delivery of the seeded cross-project behavioral/UX-safety defaults (before-authoring-interaction)
